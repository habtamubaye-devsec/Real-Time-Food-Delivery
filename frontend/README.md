## Frontend (React)

### Requirements

- Node.js + npm

### Environment

- Optional: create `frontend/.env` using [frontend/.env.example](.env.example)
- `REACT_APP_API_URL` is used by some API helpers

### Install + run

- Install: `npm install`
- Start: `npm start`

App will run at `http://localhost:3000`.
